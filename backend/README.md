# Gameflix Backend

This folder contains the Python backend for API routes, database access, services, recommendation logic, and backend tests.

Current migrated files:

- `requirements.txt`
- `tests/test_placeholder.py`

The `app/` package is ready for FastAPI modules such as `main.py`, `routes/`, `services/`, `schemas/`, and `database/`.
