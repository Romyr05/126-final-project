# Gameflix Frontend

This folder contains the Next.js frontend.

Run locally:

```bash
npm run dev
```

The app code was moved from the repo root into this folder:

- `app/`
- `components/`
- `lib/`
- `public/`
- Next.js, TypeScript, ESLint, PostCSS, and package files
